PORT=5001

# Generate with: openssl rand -base64 32
JWT_SECRET=paste-openssl-output-here

# Atlas: Database Access → user password (NOT your Atlas login password)
# Add /rental before ?appName=...
MONGODB_URI=mongodb+srv://loganathanofficial25_db_user:YOUR_ATLAS_DB_USER_PASSWORD@cluster0.gcroq8r.mongodb.net/rental?appName=Cluster0

# Razorpay (Dashboard → Settings → API Keys). Secret stays on server only.
RAZORPAY_KEY_ID=rzp_test_your_key_id
RAZORPAY_KEY_SECRET=your_key_secret

# Platform commission taken from each successful payment (percent)
COMMISSION_PERCENTAGE=10
